apply plugin: 'com.android.library'
apply plugin: 'com.novoda.bintray-release'

android {
    compileSdkVersion 28

    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 28
        versionCode 102
        versionName "1.0.2"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    lintOptions {
        abortOnError false
    }

    publish {
        userOrg = 'liweijieok'
        groupId = 'com.github.liweijie'
        artifactId = 'ThreadExecutor'
        publishVersion = '1.0.0'
        desc="ThreadManager"
        website = 'https://github.com/liweijieok/ThreadExecutor'
    }

}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
}


